# Figures

Thư mục này chứa các hình do notebook hoặc `src/reporting.py` tạo từ lượt chạy thật:

- `augmentation_examples.png`: ảnh train baseline và ba biến thể augmentation.
- `train_validation_loss.png`: loss train/validation của E1–E4.
- `validation_mae.png`: validation MAE theo epoch.
- `validation_comparison.png`: validation MAE tốt nhất của bốn thí nghiệm.

Không commit hình minh họa tạo từ dữ liệu giả như kết quả coursework. Kiểm tra trực quan vùng phổi, hướng ảnh và biên ảnh trước khi dùng hình trong báo cáo.
